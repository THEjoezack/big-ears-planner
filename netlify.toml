[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (harmless for a single-page app; helps if you add routes later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
